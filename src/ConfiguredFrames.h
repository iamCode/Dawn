#ifndef CONFIGUREDFRAMES_H
#define CONFIGUREDFRAMES_H

#include "ConfigurableFrame.h"
#include <memory>

namespace ConfiguredFrames
{
	void fillOptionsFrame( ConfigurableFrame *optionScreen );
	void fillMainMenuFrame( ConfigurableFrame *mainMenuFrame );
	void fillChooseClassFrame( ConfigurableFrame *chooseClassFrame );
}

#endif // CONFIGUREDFRAMES_H
