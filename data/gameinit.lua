--note: this is just for debug the items need to be copied not accessed directly else the same reference is used!!!
DawnInterface.giveItemToPlayer( itemDatabase["A wooden shield"] );
DawnInterface.giveItemToPlayer( itemDatabase["A rusty sword"] );
DawnInterface.giveItemToPlayer( itemDatabase["Sword of Khazom"] );
DawnInterface.giveItemToPlayer( itemDatabase["Shortsword"] );
DawnInterface.giveItemToPlayer( itemDatabase["Eye of Licor"] );
DawnInterface.giveItemToPlayer( itemDatabase["Gnoll shield"] );
DawnInterface.giveItemToPlayer( itemDatabase["Gutteraxe"] );
DawnInterface.giveItemToPlayer( itemDatabase["Snakeloop"] );


--note: this is just for debug the items need to be copied not accessed directly else the same reference is used!!!
DawnInterface.giveItemToPlayer( itemDatabase["Gutteraxe"] );

